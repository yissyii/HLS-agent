# 模型上下文与技能选择

状态：已实现；上下文采用明确标记的保守字节预算，技能检索默认关闭。

- `builder.py`：组装首次生成及修复提示词，筛选允许提供给模型的材料，计算上下文预算并记录来源。
- `skills.py`：按错误类别和关键词检索少量技能，检查版本、来源与独立验证声明。当前未附带已验证规则。

知识内容位于项目根目录的 `skill/`，本目录保存选择与组装逻辑。组件只返回上下文，不自行调用模型、执行 Vitis 或修改技能库。
