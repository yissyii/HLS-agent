# 核心流程与决策

状态：已实现，旧评测命令与新入口共用控制器。

- `contracts.py`：题目、候选、诊断、预算、验证结果及模型/工具接口的公共类型。
- `controller.py`：组织生成、验证、诊断、修复和最终交付。
- `policy.py`：根据预算、错误类别与历史进展决定继续或停止。

策略参数来自 `agent/config/policy.json`。公共类型不依赖控制器；控制器通过注入的组件调用模型和验证器。
