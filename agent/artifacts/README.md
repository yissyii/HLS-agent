# 运行证据与回执

状态：已实现。

- `writer.py`：保存输入/配置快照、请求、响应、候选源码、事件和日志索引，输出 `result.json`。
- 保存失败及结果未知的请求证据，并保留配对所需的运行编号和哈希。
- 接收候选管理模块的选择结果，写出最终 `candidate.cpp`；不自行决定选择哪个候选。

本目录只保存记录代码。实际产物全部写入本次 `<output_directory>`，不能写回 agent 源码目录。
