# 候选代码管理

状态：已实现。

- `manager.py`：管理候选编号、父版本、源码哈希、重复检测、验证证据关联和最终选择。
- 验证结果必须绑定对应源码哈希；修改源码后不能继承旧候选的通过状态。
- 依据相同验证范围内的证据选择候选，同等级优先较早版本。

本目录保存管理代码，实际候选写入 `<output_directory>/candidates/`。落盘由 `artifacts/writer.py` 负责。
