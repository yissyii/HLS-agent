Translate ONLY the public problem into a bounded scalar bit-vector/state specification. Do not generate C++, Python programs, expected-value lists or diagnostic code. The deterministic interpreter calculates expected values and emits a fixed C++ testbench. Candidate implementation, official tests and outcomes are unavailable.

The supplied interface is authoritative. Its inputs have aliases i0,i1,... in prototype order (use aliases even when the public name is a Python keyword such as in). Outputs use their exact public names. Only bool/ap_int/ap_uint scalar interfaces are supported. Keep interface types unchanged.

Return exactly one JSON object with these keys:
{"schema_version":1,"decision":"ready","state":[],"outputs":[{"name":"public_output_name","expr":"integer expression","when":"True","evidence":["exact literal public quotation"]}],"sequences":[],"excluded":[],"reasons":[]}

Expressions use ONLY integer/boolean constants, aliases, parentheses, + - * % & | ^ ~ << >>, == != < <= > >=, not/and/or, and Python conditional 'value_if_true if condition else value_if_false'. No calls, indexing, attributes, C++ syntax, loops, floats, division, exponentiation or assignments. Use (i0 >> k) & 1 for a bit. Intermediate arithmetic uses mathematical integers; output/state assignments truncate to declared bit width (signed outputs then decode two's complement). Shifts are bounded 0..1024. Expressions must stay compact; at most 128 AST nodes each.

For stateful behavior define state entries {"name":"s0","width":8,"next":"...","evidence":["literal quote"]}. At most 8 states, named s0,s1,...; these are register variables, not necessarily FSM enumerated states. State starts UNKNOWN, never assumed zero. All next expressions evaluate against OLD state and current inputs simultaneously. Outputs may use s0 (old state) or n0 (updated s0), with the corresponding n1 etc. Use whichever observation/update ordering the public problem specifies; do not guess missing timing conventions. Lazy conditional expressions and boolean logic can resolve unknown state after reset or a synchronizing write. No explicit state/clock argument is needed for internal static state. An output reference is not automatically caller-controlled state storage.

You may add at most 16 directed sequence entries {"inputs":[integer input values in prototype input order],"repeat":positive_integer_up_to_128}, up to 256 total calls. Use these to synchronize state, exercise wraparound and state transitions. The framework additionally generates exhaustive small input domains or deterministic wide-vector boundary/random stimuli. It checks every known, enabled output; unknown outputs are skipped until state is known. Every output must eventually have a meaningful check.

For don't-cares, set each output's 'when' predicate false only on the undefined inputs. Do not turn undefined behavior into an assertion. All scalar inputs must fit the supplied widths; booleans in sequences are integers 0/1. 'when' may reference input/state/next aliases. Never use a predicate to hide a defined mismatch.

Every state and output needs 1-4 exact nonempty quotations from public_problem (not paraphrases). Evidence proves provenance only. ready means no known semantic exclusions, not exhaustive coverage; partial requires a nonempty excluded list. If transition/order rules cannot be expressed soundly, return decision abstain with empty state, outputs and sequences, and nonempty reasons. Preserve uncertainty. Do not invent initial values, reset values, or delay conventions. Keep output compact enough for 4096 tokens.
