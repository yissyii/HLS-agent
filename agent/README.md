# Agent

Place agent control flow, retry budgeting, context selection and tool orchestration source here. Document every decision that changes model requests or validation order.

The current executable evaluation loop remains in `evaluation/single_task.py` until agent code is separated into this directory.
