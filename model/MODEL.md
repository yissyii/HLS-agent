# Model declaration

Record the model repository or weight checksum, version, quantization, measured single-GPU memory use, context configuration and any fine-tuning provenance here.

Do not commit model weights. Place local weights under `model/weights/`, which is ignored by Git.
