# Data

Commit dataset manifests, provenance records, split definitions and scripts that reproduce local data preparation. Keep raw datasets under `data/raw/` and processed datasets under `data/processed/`; both are ignored by Git.

Separate development, validation, frozen-test and fine-tuning data. Do not commit hidden-test answers or credentials.
