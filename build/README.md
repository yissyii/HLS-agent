# Build

Commit reproducible container and build scripts here. Generated build products belong in `build/artifacts/`, which is ignored by Git.

Document tool version, target part, clock constraint and command line for every reproducible build.
