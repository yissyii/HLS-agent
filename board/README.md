# Board and implementation

Record board-specific constraints, implementation scripts and reproducible run instructions here. Generated reports and implementation outputs belong in `board/artifacts/`, which is ignored by Git.

Do not claim place-and-route results from HLS synthesis estimates.
